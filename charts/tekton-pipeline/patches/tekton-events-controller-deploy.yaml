apiVersion: apps/v1
kind: Deployment
metadata:
  name: tekton-events-controller
spec:
  template:
    spec:
      containers:
      - name: tekton-events-controller
      helmTemplateRemoveMe: |
        image: {{ .Values.eventscontroller.deployment.image }}
